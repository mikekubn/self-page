import React from 'react';
import type { NextPage } from 'next';
import Head from 'next/head';
import Link from 'next/link';
import { useRouter } from 'next/router';

const navigationConfig = [
  { name: 'Technology', url: '/#technology' },
  { name: 'Contact', url: '/#contact' },
];

const Home: NextPage = (): React.ReactElement => {
  const router = useRouter();

  return (
    <>
      <Head>
        <title>Michael Kubín</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.png" />
      </Head>
      <section id="home" className="grid grid-cols-2 h-[100vh]">
        {
        /**
         * Section Programmer
         */
      }
        <div className="flex flex-col flex-1 pl-24">
          <div className="flex flex-col justify-center flex-1">
            <h1 className="pt-5 pb-10 text-6xl font-Asap">Michael Kubín</h1>
            <p className="text-3xl font-AsapItal">Frontend developer</p>
          </div>
          {
        /**
         * Section Navigation
         */
        }
          <div className="flex flex-row pb-20">
            {
            navigationConfig.map((nav) => (
              <Link href={nav.url} key={nav.url} scroll={false} replace passHref>
                <div className="mr-10 w-[200px] h-[50px]">
                  <button type="button" className="bg-transparent border rounded-full cursor-pointer text-sky500 w-[100%] h-[100%] hover:w-[110%]">
                    {nav.name}
                  </button>
                </div>
              </Link>
            ))
          }
          </div>
        </div>
        {
        /**
         * Section Timeline
         */
      }
        <div className="flex flex-col justify-start flex-1 pt-24">
          <h1 className="mx-auto text-3xl font-AsapItal">Work experience</h1>
          <div className="flex flex-1 bg-pink" />
        </div>
      </section>
      <section id="technology" className="h-[100vh] bg-green" />
      <section id="contact" className="h-[100vh] bg-purple" />
    </>
  );
};

export default Home;
